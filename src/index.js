import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // assuming Tailwind CSS is included in this file
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
